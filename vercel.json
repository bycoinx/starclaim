{
  "buildCommand": "cd frontend && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "frontend/build",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://starclaim.onrender.com/api/:path*"
    },
    {
      "source": "/((?!static|.*\\..*|api).*)",
      "destination": "/index.html"
    }
  ]
}
